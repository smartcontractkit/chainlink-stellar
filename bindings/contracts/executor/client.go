// Code generated by stellar-bindings-generator. DO NOT EDIT.
package executor

import (
	"context"
	"fmt"
	"time"

	"github.com/smartcontractkit/chainlink-stellar/bindings"
	"github.com/smartcontractkit/chainlink-stellar/bindings/scval"
	protocolrpc "github.com/stellar/go-stellar-sdk/protocols/rpc"
	"github.com/stellar/go-stellar-sdk/xdr"
)

// ExecutorClient provides methods to interact with the Executor contract.
type ExecutorClient struct {
	invoker    bindings.Invoker
	contractID string
}

// NewExecutorClient creates a new ExecutorClient.
func NewExecutorClient(invoker bindings.Invoker, contractID string) *ExecutorClient {
	return &ExecutorClient{
		invoker:    invoker,
		contractID: contractID,
	}
}

// ContractID returns the contract ID.
func (c *ExecutorClient) ContractID() string {
	return c.contractID
}

// Owner calls the owner function on the contract.
func (c *ExecutorClient) Owner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetFee calls the get_fee function on the contract.
func (c *ExecutorClient) GetFee(ctx context.Context, destChainSelector uint64, requestedFinalityConfig uint32, ccvAddresses []string, extraArgs []byte, feeToken string) (uint32, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
		scval.Uint32ToScVal(requestedFinalityConfig),
		scval.AddressSliceToScVal(ccvAddresses),
		scval.BytesToScVal(extraArgs),
		scval.AddressToScVal(feeToken),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_fee", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_fee: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_fee")
	}

	v, ok := result.GetU32()
	if !ok {
		return 0, fmt.Errorf("expected u32 return type")
	}
	return uint32(v), nil
}

// Upgrade calls the upgrade function on the contract.
func (c *ExecutorClient) Upgrade(ctx context.Context, newWasmHash [32]byte) error {
	args := []xdr.ScVal{
		scval.Bytes32ToScVal(newWasmHash),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "upgrade", args)
	if err != nil {
		return fmt.Errorf("failed to call upgrade: %w", err)
	}

	_ = result // void return
	return nil
}

// IsOwner calls the is_owner function on the contract.
func (c *ExecutorClient) IsOwner(ctx context.Context, addr string) (bool, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(addr),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "is_owner", args)
	if err != nil {
		return false, fmt.Errorf("failed to call is_owner: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from is_owner")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// InitOwner calls the init_owner function on the contract.
func (c *ExecutorClient) InitOwner(ctx context.Context, owner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "init_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call init_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// Initialize calls the initialize function on the contract.
func (c *ExecutorClient) Initialize(ctx context.Context, owner string, maxCcvsPerMsg uint32, dynamicConfig DynamicConfig) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
		scval.Uint32ToScVal(maxCcvsPerMsg),
		scval.MustToScVal(dynamicConfig.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "initialize", args)
	if err != nil {
		return fmt.Errorf("failed to call initialize: %w", err)
	}

	_ = result // void return
	return nil
}

// RequireOwner calls the require_owner function on the contract.
func (c *ExecutorClient) RequireOwner(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "require_owner", args)
	if err != nil {
		return "", fmt.Errorf("failed to call require_owner: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from require_owner")
	}

	v, err := scval.AddressFromScVal(*result)
	if err != nil {
		return "", err
	}
	return v, nil
}

// GetDestChains calls the get_dest_chains function on the contract.
func (c *ExecutorClient) GetDestChains(ctx context.Context) ([]RemoteChainConfigArgs, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_dest_chains", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_dest_chains: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_dest_chains")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return nil, fmt.Errorf("expected vec return type")
	}
	out := make([]RemoteChainConfigArgs, len(*vec))
	for i, item := range *vec {
		v, err := RemoteChainConfigArgsFromScVal(item)
		if err != nil {
			return nil, err
		}
		out[i] = *v
	}
	return out, nil
}

// AcceptOwnership calls the accept_ownership function on the contract.
func (c *ExecutorClient) AcceptOwnership(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "accept_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call accept_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllowedCcvs calls the get_allowed_ccvs function on the contract.
func (c *ExecutorClient) GetAllowedCcvs(ctx context.Context) ([]string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_allowed_ccvs", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_allowed_ccvs: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_allowed_ccvs")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return nil, fmt.Errorf("expected vec return type")
	}
	out := make([]string, len(*vec))
	for i, item := range *vec {
		v, err := scval.AddressFromScVal(item)
		if err != nil {
			return nil, err
		}
		out[i] = v
	}
	return out, nil
}

// TypeAndVersion calls the type_and_version function on the contract.
func (c *ExecutorClient) TypeAndVersion(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "type_and_version", args)
	if err != nil {
		return "", fmt.Errorf("failed to call type_and_version: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from type_and_version")
	}

	return scval.StringFromScVal(*result)
}

// GetPendingOwner calls the get_pending_owner function on the contract.
func (c *ExecutorClient) GetPendingOwner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_pending_owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_pending_owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_pending_owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetDynamicConfig calls the get_dynamic_config function on the contract.
func (c *ExecutorClient) GetDynamicConfig(ctx context.Context) (*DynamicConfig, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_dynamic_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_dynamic_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_dynamic_config")
	}

	return DynamicConfigFromScVal(*result)
}

// SetDynamicConfig calls the set_dynamic_config function on the contract.
func (c *ExecutorClient) SetDynamicConfig(ctx context.Context, dynamicConfig DynamicConfig) error {
	args := []xdr.ScVal{
		scval.MustToScVal(dynamicConfig.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_dynamic_config", args)
	if err != nil {
		return fmt.Errorf("failed to call set_dynamic_config: %w", err)
	}

	_ = result // void return
	return nil
}

// TransferOwnership calls the transfer_ownership function on the contract.
func (c *ExecutorClient) TransferOwnership(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "transfer_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call transfer_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// WithdrawFeeTokens calls the withdraw_fee_tokens function on the contract.
func (c *ExecutorClient) WithdrawFeeTokens(ctx context.Context, feeTokens []string) error {
	args := []xdr.ScVal{
		scval.AddressSliceToScVal(feeTokens),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "withdraw_fee_tokens", args)
	if err != nil {
		return fmt.Errorf("failed to call withdraw_fee_tokens: %w", err)
	}

	_ = result // void return
	return nil
}

// GetDestChainConfig calls the get_dest_chain_config function on the contract.
func (c *ExecutorClient) GetDestChainConfig(ctx context.Context, destChainSelector uint64) (*RemoteChainConfig, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_dest_chain_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_dest_chain_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_dest_chain_config")
	}

	return RemoteChainConfigFromScVal(*result)
}

// ApplyDestChainUpdates calls the apply_dest_chain_updates function on the contract.
func (c *ExecutorClient) ApplyDestChainUpdates(ctx context.Context, destChainSelectorsToRemove []uint64, destChainSelectorsToAdd []RemoteChainConfigArgs) error {
	args := []xdr.ScVal{
		scval.Uint64SliceToScVal(destChainSelectorsToRemove),
		scval.StructSliceToScVal(destChainSelectorsToAdd),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "apply_dest_chain_updates", args)
	if err != nil {
		return fmt.Errorf("failed to call apply_dest_chain_updates: %w", err)
	}

	_ = result // void return
	return nil
}

// GetMaxCcvsPerMessage calls the get_max_ccvs_per_message function on the contract.
func (c *ExecutorClient) GetMaxCcvsPerMessage(ctx context.Context) (uint32, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_max_ccvs_per_message", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_max_ccvs_per_message: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_max_ccvs_per_message")
	}

	v, ok := result.GetU32()
	if !ok {
		return 0, fmt.Errorf("expected u32 return type")
	}
	return uint32(v), nil
}

// ApplyAllowedCcvUpdates calls the apply_allowed_ccv_updates function on the contract.
func (c *ExecutorClient) ApplyAllowedCcvUpdates(ctx context.Context, ccvsToRemove []string, ccvsToAdd []string, ccvAllowlistEnabled bool) error {
	args := []xdr.ScVal{
		scval.AddressSliceToScVal(ccvsToRemove),
		scval.AddressSliceToScVal(ccvsToAdd),
		scval.BoolToScVal(ccvAllowlistEnabled),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "apply_allowed_ccv_updates", args)
	if err != nil {
		return fmt.Errorf("failed to call apply_allowed_ccv_updates: %w", err)
	}

	_ = result // void return
	return nil
}

// CancelOwnershipTransfer calls the cancel_ownership_transfer function on the contract.
func (c *ExecutorClient) CancelOwnershipTransfer(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "cancel_ownership_transfer", args)
	if err != nil {
		return fmt.Errorf("failed to call cancel_ownership_transfer: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllowedFinalityConfig calls the get_allowed_finality_config function on the contract.
func (c *ExecutorClient) GetAllowedFinalityConfig(ctx context.Context) (uint32, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_allowed_finality_config", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_allowed_finality_config: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_allowed_finality_config")
	}

	v, ok := result.GetU32()
	if !ok {
		return 0, fmt.Errorf("expected u32 return type")
	}
	return uint32(v), nil
}

// WaitForRoleGrantedEvent waits for a RoleGrantedEvent event.
func (c *ExecutorClient) WaitForRoleGrantedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleGrantedEvent) bool) (*RoleGrantedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleGrantedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleGrantedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleGrantedEvent(e protocolrpc.EventInfo) (*RoleGrantedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleGrantedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForRoleRevokedEvent waits for a RoleRevokedEvent event.
func (c *ExecutorClient) WaitForRoleRevokedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleRevokedEvent) bool) (*RoleRevokedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleRevokedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleRevokedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleRevokedEvent(e protocolrpc.EventInfo) (*RoleRevokedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleRevokedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerAddedEvent waits for a AuthorizedCallerAddedEvent event.
func (c *ExecutorClient) WaitForAuthorizedCallerAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerAddedEvent) bool) (*AuthorizedCallerAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerAddedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerRemovedEvent waits for a AuthorizedCallerRemovedEvent event.
func (c *ExecutorClient) WaitForAuthorizedCallerRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerRemovedEvent) bool) (*AuthorizedCallerRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerRemovedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransferStartedEvent waits for a OwnershipTransferStartedEvent event.
func (c *ExecutorClient) WaitForOwnershipTransferStartedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferStartedEvent) bool) (*OwnershipTransferStartedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferStartedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipTransferStartedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipTransferStartedEvent(e protocolrpc.EventInfo) (*OwnershipTransferStartedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferStartedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "previous_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.PreviousOwner = v
			}
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}

// WaitForCCVAddedEvent waits for a CCVAddedEvent event.
func (c *ExecutorClient) WaitForCCVAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CCVAddedEvent) bool) (*CCVAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CCVAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseCCVAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseCCVAddedEvent(e protocolrpc.EventInfo) (*CCVAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CCVAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "ccv":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Ccv = v
			}
		}
	}

	return result, nil
}

// WaitForConfigSetEvent waits for a ConfigSetEvent event.
func (c *ExecutorClient) WaitForConfigSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*ConfigSetEvent) bool) (*ConfigSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{ConfigSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseConfigSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseConfigSetEvent(e protocolrpc.EventInfo) (*ConfigSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &ConfigSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "dynamic_config":
			v, err := DynamicConfigFromScVal(entry.Val)
			if err == nil {
				result.DynamicConfig = *v
			}
		}
	}

	return result, nil
}

// WaitForCCVRemovedEvent waits for a CCVRemovedEvent event.
func (c *ExecutorClient) WaitForCCVRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CCVRemovedEvent) bool) (*CCVRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CCVRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseCCVRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseCCVRemovedEvent(e protocolrpc.EventInfo) (*CCVRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CCVRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "ccv":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Ccv = v
			}
		}
	}

	return result, nil
}

// WaitForDestChainAddedEvent waits for a DestChainAddedEvent event.
func (c *ExecutorClient) WaitForDestChainAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*DestChainAddedEvent) bool) (*DestChainAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{DestChainAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseDestChainAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseDestChainAddedEvent(e protocolrpc.EventInfo) (*DestChainAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &DestChainAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "dest_chain_selector":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.DestChainSelector = v
			}
		case "config":
			v, err := RemoteChainConfigFromScVal(entry.Val)
			if err == nil {
				result.Config = *v
			}
		}
	}

	return result, nil
}

// WaitForDestChainRemovedEvent waits for a DestChainRemovedEvent event.
func (c *ExecutorClient) WaitForDestChainRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*DestChainRemovedEvent) bool) (*DestChainRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{DestChainRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseDestChainRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseDestChainRemovedEvent(e protocolrpc.EventInfo) (*DestChainRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &DestChainRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "dest_chain_selector":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.DestChainSelector = v
			}
		}
	}

	return result, nil
}

// WaitForFeeTokenWithdrawnEvent waits for a FeeTokenWithdrawnEvent event.
func (c *ExecutorClient) WaitForFeeTokenWithdrawnEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*FeeTokenWithdrawnEvent) bool) (*FeeTokenWithdrawnEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{FeeTokenWithdrawnEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseFeeTokenWithdrawnEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseFeeTokenWithdrawnEvent(e protocolrpc.EventInfo) (*FeeTokenWithdrawnEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &FeeTokenWithdrawnEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "receiver":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Receiver = v
			}
		case "fee_token":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.FeeToken = v
			}
		case "amount":
			v, err := scval.I128FromScVal(entry.Val)
			if err == nil {
				result.Amount = v
			}
		}
	}

	return result, nil
}

// WaitForCCVAllowlistUpdatedEvent waits for a CCVAllowlistUpdatedEvent event.
func (c *ExecutorClient) WaitForCCVAllowlistUpdatedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CCVAllowlistUpdatedEvent) bool) (*CCVAllowlistUpdatedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CCVAllowlistUpdatedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseCCVAllowlistUpdatedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseCCVAllowlistUpdatedEvent(e protocolrpc.EventInfo) (*CCVAllowlistUpdatedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CCVAllowlistUpdatedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "enabled":
			v, ok := entry.Val.GetB()
			if ok {
				result.Enabled = v
			}
		}
	}

	return result, nil
}
