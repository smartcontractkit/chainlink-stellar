// Code generated by stellar-bindings-generator. DO NOT EDIT.
package data_feeds_proxy

import (
	"context"
	"fmt"
	"time"

	"github.com/smartcontractkit/chainlink-stellar/bindings"
	"github.com/smartcontractkit/chainlink-stellar/bindings/scval"
	protocolrpc "github.com/stellar/go-stellar-sdk/protocols/rpc"
	"github.com/stellar/go-stellar-sdk/xdr"
)

// DataFeedsProxyClient provides methods to interact with the DataFeedsProxy contract.
type DataFeedsProxyClient struct {
	invoker    bindings.Invoker
	contractID string
}

// NewDataFeedsProxyClient creates a new DataFeedsProxyClient.
func NewDataFeedsProxyClient(invoker bindings.Invoker, contractID string) *DataFeedsProxyClient {
	return &DataFeedsProxyClient{
		invoker:    invoker,
		contractID: contractID,
	}
}

// ContractID returns the contract ID.
func (c *DataFeedsProxyClient) ContractID() string {
	return c.contractID
}

// Upgrade builds a call to the upgrade contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) Upgrade(newWasmHash WasmHash) *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{
		scval.MustToScVal(newWasmHash.ToScVal()),
	}
	return bindings.NewCall(c.invoker, c.contractID, "upgrade", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// Version builds a call to the version contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) Version() *bindings.Call[uint32] {
	args := []xdr.ScVal{}
	return bindings.NewCall(c.invoker, c.contractID, "version", args, func(result *xdr.ScVal) (uint32, error) {
		if result == nil {
			return 0, fmt.Errorf("no return value from version")
		}
		v, ok := result.GetU32()
		if !ok {
			return 0, fmt.Errorf("expected u32 return type")
		}
		return uint32(v), nil
	})
}

// Decimals builds a call to the decimals contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) Decimals(dataId DataId) *bindings.Call[uint32] {
	args := []xdr.ScVal{
		scval.MustToScVal(dataId.ToScVal()),
	}
	return bindings.NewCall(c.invoker, c.contractID, "decimals", args, func(result *xdr.ScVal) (uint32, error) {
		if result == nil {
			return 0, fmt.Errorf("no return value from decimals")
		}
		v, ok := result.GetU32()
		if !ok {
			return 0, fmt.Errorf("expected u32 return type")
		}
		return uint32(v), nil
	})
}

// GetOwner builds a call to the get_owner contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) GetOwner() *bindings.Call[*string] {
	args := []xdr.ScVal{}
	return bindings.NewCall(c.invoker, c.contractID, "get_owner", args, func(result *xdr.ScVal) (*string, error) {
		if result == nil {
			return nil, fmt.Errorf("no return value from get_owner")
		}
		v, err := scval.OptionalAddressFromScVal(*result)
		if err != nil {
			return nil, err
		}
		return v, nil
	})
}

// GetRound builds a call to the get_round contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) GetRound(dataId DataId, roundId uint64) *bindings.Call[*Round] {
	args := []xdr.ScVal{
		scval.MustToScVal(dataId.ToScVal()),
		scval.Uint64ToScVal(roundId),
	}
	return bindings.NewCall(c.invoker, c.contractID, "get_round", args, func(result *xdr.ScVal) (*Round, error) {
		if result == nil {
			return nil, fmt.Errorf("no return value from get_round")
		}
		return RoundFromScVal(*result)
	})
}

// SetCache builds a call to the set_cache contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) SetCache(cache string) *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{
		scval.AddressToScVal(cache),
	}
	return bindings.NewCall(c.invoker, c.contractID, "set_cache", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// Description builds a call to the description contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) Description(dataId DataId) *bindings.Call[string] {
	args := []xdr.ScVal{
		scval.MustToScVal(dataId.ToScVal()),
	}
	return bindings.NewCall(c.invoker, c.contractID, "description", args, func(result *xdr.ScVal) (string, error) {
		if result == nil {
			return "", fmt.Errorf("no return value from description")
		}
		return scval.StringFromScVal(*result)
	})
}

// LatestRound builds a call to the latest_round contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) LatestRound(dataId DataId) *bindings.Call[*Round] {
	args := []xdr.ScVal{
		scval.MustToScVal(dataId.ToScVal()),
	}
	return bindings.NewCall(c.invoker, c.contractID, "latest_round", args, func(result *xdr.ScVal) (*Round, error) {
		if result == nil {
			return nil, fmt.Errorf("no return value from latest_round")
		}
		return RoundFromScVal(*result)
	})
}

// RecoverTokens builds a call to the recover_tokens contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) RecoverTokens(token string, to string, amount int64) *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{
		scval.AddressToScVal(token),
		scval.AddressToScVal(to),
		scval.I128ToScVal(amount),
	}
	return bindings.NewCall(c.invoker, c.contractID, "recover_tokens", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// AcceptOwnership builds a call to the accept_ownership contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) AcceptOwnership() *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{}
	return bindings.NewCall(c.invoker, c.contractID, "accept_ownership", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// TypeAndVersion builds a call to the type_and_version contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) TypeAndVersion() *bindings.Call[string] {
	args := []xdr.ScVal{}
	return bindings.NewCall(c.invoker, c.contractID, "type_and_version", args, func(result *xdr.ScVal) (string, error) {
		if result == nil {
			return "", fmt.Errorf("no return value from type_and_version")
		}
		return scval.StringFromScVal(*result)
	})
}

// RenounceOwnership builds a call to the renounce_ownership contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) RenounceOwnership() *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{}
	return bindings.NewCall(c.invoker, c.contractID, "renounce_ownership", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// TransferOwnership builds a call to the transfer_ownership contract function.
// Execute it with Result (free simulation, state changes discarded) or
// SignAndSend (fee-paying transaction).
func (c *DataFeedsProxyClient) TransferOwnership(newOwner string, liveUntilLedger uint32) *bindings.Call[bindings.Void] {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
		scval.Uint32ToScVal(liveUntilLedger),
	}
	return bindings.NewCall(c.invoker, c.contractID, "transfer_ownership", args, func(result *xdr.ScVal) (bindings.Void, error) {
		_ = result // void return
		return bindings.Void{}, nil
	})
}

// WaitForCacheSet waits for a CacheSet event.
func (c *DataFeedsProxyClient) WaitForCacheSet(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CacheSet) bool) (*CacheSet, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CacheSetTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseCacheSet(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseCacheSet(e protocolrpc.EventInfo) (*CacheSet, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CacheSet{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "old_cache":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.OldCache = v
			}
		case "new_cache":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewCache = v
			}
		}
	}

	return result, nil
}

// WaitForUpgraded waits for a Upgraded event.
func (c *DataFeedsProxyClient) WaitForUpgraded(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*Upgraded) bool) (*Upgraded, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{UpgradedTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseUpgraded(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseUpgraded(e protocolrpc.EventInfo) (*Upgraded, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &Upgraded{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "new_wasm_hash":
			v, err := WasmHashFromScVal(entry.Val)
			if err == nil {
				result.NewWasmHash = *v
			}
		}
	}

	return result, nil
}

// WaitForTokenRecovered waits for a TokenRecovered event.
func (c *DataFeedsProxyClient) WaitForTokenRecovered(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*TokenRecovered) bool) (*TokenRecovered, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{TokenRecoveredTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseTokenRecovered(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseTokenRecovered(e protocolrpc.EventInfo) (*TokenRecovered, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &TokenRecovered{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "token":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Token = v
			}
		case "to":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.To = v
			}
		case "amount":
			v, err := scval.I128FromScVal(entry.Val)
			if err == nil {
				result.Amount = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransfer waits for a OwnershipTransfer event.
func (c *DataFeedsProxyClient) WaitForOwnershipTransfer(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransfer) bool) (*OwnershipTransfer, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipTransfer(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipTransfer(e protocolrpc.EventInfo) (*OwnershipTransfer, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransfer{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "old_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.OldOwner = v
			}
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		case "live_until_ledger":
			v, ok := entry.Val.GetU32()
			if ok {
				result.LiveUntilLedger = uint32(v)
			}
		}
	}

	return result, nil
}

// WaitForOwnershipRenounced waits for a OwnershipRenounced event.
func (c *DataFeedsProxyClient) WaitForOwnershipRenounced(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipRenounced) bool) (*OwnershipRenounced, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipRenouncedTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipRenounced(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipRenounced(e protocolrpc.EventInfo) (*OwnershipRenounced, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipRenounced{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "old_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.OldOwner = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransferCompleted waits for a OwnershipTransferCompleted event.
func (c *DataFeedsProxyClient) WaitForOwnershipTransferCompleted(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferCompleted) bool) (*OwnershipTransferCompleted, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferCompletedTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipTransferCompleted(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipTransferCompleted(e protocolrpc.EventInfo) (*OwnershipTransferCompleted, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferCompleted{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}
