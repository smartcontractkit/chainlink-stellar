// Code generated by stellar-bindings-generator. DO NOT EDIT.
package advanced_pool_hooks

import (
	"context"
	"fmt"
	"math/big"
	"time"

	"github.com/smartcontractkit/chainlink-stellar/bindings"
	"github.com/smartcontractkit/chainlink-stellar/bindings/scval"
	protocolrpc "github.com/stellar/go-stellar-sdk/protocols/rpc"
	"github.com/stellar/go-stellar-sdk/xdr"
)

// AdvancedPoolHooksClient provides methods to interact with the AdvancedPoolHooks contract.
type AdvancedPoolHooksClient struct {
	invoker    bindings.Invoker
	contractID string
}

// NewAdvancedPoolHooksClient creates a new AdvancedPoolHooksClient.
func NewAdvancedPoolHooksClient(invoker bindings.Invoker, contractID string) *AdvancedPoolHooksClient {
	return &AdvancedPoolHooksClient{
		invoker:    invoker,
		contractID: contractID,
	}
}

// ContractID returns the contract ID.
func (c *AdvancedPoolHooksClient) ContractID() string {
	return c.contractID
}

// Owner calls the owner function on the contract.
func (c *AdvancedPoolHooksClient) Owner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// IsOwner calls the is_owner function on the contract.
func (c *AdvancedPoolHooksClient) IsOwner(ctx context.Context, addr string) (bool, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(addr),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "is_owner", args)
	if err != nil {
		return false, fmt.Errorf("failed to call is_owner: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from is_owner")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// InitOwner calls the init_owner function on the contract.
func (c *AdvancedPoolHooksClient) InitOwner(ctx context.Context, owner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "init_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call init_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// Initialize calls the initialize function on the contract.
func (c *AdvancedPoolHooksClient) Initialize(ctx context.Context, owner string, allowlist []string, thresholdAmount *big.Int) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
		scval.AddressSliceToScVal(allowlist),
		scval.I128ToScVal(thresholdAmount),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "initialize", args)
	if err != nil {
		return fmt.Errorf("failed to call initialize: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllowlist calls the get_allowlist function on the contract.
func (c *AdvancedPoolHooksClient) GetAllowlist(ctx context.Context) ([]string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_allowlist", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_allowlist: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_allowlist")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return nil, fmt.Errorf("expected vec return type")
	}
	out := make([]string, len(*vec))
	for i, item := range *vec {
		v, err := scval.AddressFromScVal(item)
		if err != nil {
			return nil, err
		}
		out[i] = v
	}
	return out, nil
}

// RequireOwner calls the require_owner function on the contract.
func (c *AdvancedPoolHooksClient) RequireOwner(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "require_owner", args)
	if err != nil {
		return "", fmt.Errorf("failed to call require_owner: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from require_owner")
	}

	v, err := scval.AddressFromScVal(*result)
	if err != nil {
		return "", err
	}
	return v, nil
}

// GetCcvConfig calls the get_ccv_config function on the contract.
func (c *AdvancedPoolHooksClient) GetCcvConfig(ctx context.Context, remoteChainSelector uint64) (*CCVConfig, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(remoteChainSelector),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_ccv_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_ccv_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_ccv_config")
	}

	if result.Type == xdr.ScValTypeScvVoid {
		return nil, nil
	}
	v, err := CCVConfigFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// PreflightCheck calls the preflight_check function on the contract.
func (c *AdvancedPoolHooksClient) PreflightCheck(ctx context.Context, lockOrBurnIn LockOrBurnIn, requestedFinality uint32, tokenArgs []byte, amount *big.Int) error {
	args := []xdr.ScVal{
		scval.MustToScVal(lockOrBurnIn.ToScVal()),
		scval.Uint32ToScVal(requestedFinality),
		scval.BytesToScVal(tokenArgs),
		scval.I128ToScVal(amount),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "preflight_check", args)
	if err != nil {
		return fmt.Errorf("failed to call preflight_check: %w", err)
	}

	_ = result // void return
	return nil
}

// AcceptOwnership calls the accept_ownership function on the contract.
func (c *AdvancedPoolHooksClient) AcceptOwnership(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "accept_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call accept_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// PostflightCheck calls the postflight_check function on the contract.
func (c *AdvancedPoolHooksClient) PostflightCheck(ctx context.Context, releaseOrMintIn ReleaseOrMintIn, localAmount *big.Int, requestedFinality uint32) error {
	args := []xdr.ScVal{
		scval.MustToScVal(releaseOrMintIn.ToScVal()),
		scval.I128ToScVal(localAmount),
		scval.Uint32ToScVal(requestedFinality),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "postflight_check", args)
	if err != nil {
		return fmt.Errorf("failed to call postflight_check: %w", err)
	}

	_ = result // void return
	return nil
}

// TypeAndVersion calls the type_and_version function on the contract.
func (c *AdvancedPoolHooksClient) TypeAndVersion(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "type_and_version", args)
	if err != nil {
		return "", fmt.Errorf("failed to call type_and_version: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from type_and_version")
	}

	return scval.StringFromScVal(*result)
}

// GetPendingOwner calls the get_pending_owner function on the contract.
func (c *AdvancedPoolHooksClient) GetPendingOwner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_pending_owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_pending_owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_pending_owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetRequiredCcvs calls the get_required_ccvs function on the contract.
func (c *AdvancedPoolHooksClient) GetRequiredCcvs(ctx context.Context, localToken string, remoteChainSelector uint64, amount *big.Int, requestedFinality uint32, extraData []byte, direction MessageDirection) (*PoolRequiredCCVs, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(localToken),
		scval.Uint64ToScVal(remoteChainSelector),
		scval.I128ToScVal(amount),
		scval.Uint32ToScVal(requestedFinality),
		scval.BytesToScVal(extraData),
		scval.MustToScVal(direction.ToScVal()),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_required_ccvs", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_required_ccvs: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_required_ccvs")
	}

	return PoolRequiredCCVsFromScVal(*result)
}

// TransferOwnership calls the transfer_ownership function on the contract.
func (c *AdvancedPoolHooksClient) TransferOwnership(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "transfer_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call transfer_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllCcvConfigs calls the get_all_ccv_configs function on the contract.
func (c *AdvancedPoolHooksClient) GetAllCcvConfigs(ctx context.Context) ([]CCVConfigArg, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_all_ccv_configs", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_all_ccv_configs: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_all_ccv_configs")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return nil, fmt.Errorf("expected vec return type")
	}
	out := make([]CCVConfigArg, len(*vec))
	for i, item := range *vec {
		v, err := CCVConfigArgFromScVal(item)
		if err != nil {
			return nil, err
		}
		out[i] = *v
	}
	return out, nil
}

// GetThresholdAmount calls the get_threshold_amount function on the contract.
func (c *AdvancedPoolHooksClient) GetThresholdAmount(ctx context.Context) (*big.Int, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_threshold_amount", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_threshold_amount: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_threshold_amount")
	}

	v, err := scval.I128FromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// SetThresholdAmount calls the set_threshold_amount function on the contract.
func (c *AdvancedPoolHooksClient) SetThresholdAmount(ctx context.Context, thresholdAmount *big.Int) error {
	args := []xdr.ScVal{
		scval.I128ToScVal(thresholdAmount),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_threshold_amount", args)
	if err != nil {
		return fmt.Errorf("failed to call set_threshold_amount: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllowlistEnabled calls the get_allowlist_enabled function on the contract.
func (c *AdvancedPoolHooksClient) GetAllowlistEnabled(ctx context.Context) (bool, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_allowlist_enabled", args)
	if err != nil {
		return false, fmt.Errorf("failed to call get_allowlist_enabled: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from get_allowlist_enabled")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// ApplyAllowlistUpdates calls the apply_allowlist_updates function on the contract.
func (c *AdvancedPoolHooksClient) ApplyAllowlistUpdates(ctx context.Context, removes []string, adds []string) error {
	args := []xdr.ScVal{
		scval.AddressSliceToScVal(removes),
		scval.AddressSliceToScVal(adds),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "apply_allowlist_updates", args)
	if err != nil {
		return fmt.Errorf("failed to call apply_allowlist_updates: %w", err)
	}

	_ = result // void return
	return nil
}

// ApplyCcvConfigUpdates calls the apply_ccv_config_updates function on the contract.
func (c *AdvancedPoolHooksClient) ApplyCcvConfigUpdates(ctx context.Context, configs []CCVConfigArg) error {
	args := []xdr.ScVal{
		scval.StructSliceToScVal(configs),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "apply_ccv_config_updates", args)
	if err != nil {
		return fmt.Errorf("failed to call apply_ccv_config_updates: %w", err)
	}

	_ = result // void return
	return nil
}

// CancelOwnershipTransfer calls the cancel_ownership_transfer function on the contract.
func (c *AdvancedPoolHooksClient) CancelOwnershipTransfer(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "cancel_ownership_transfer", args)
	if err != nil {
		return fmt.Errorf("failed to call cancel_ownership_transfer: %w", err)
	}

	_ = result // void return
	return nil
}

// WaitForRoleGrantedEvent waits for a RoleGrantedEvent event.
func (c *AdvancedPoolHooksClient) WaitForRoleGrantedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleGrantedEvent) bool) (*RoleGrantedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleGrantedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleGrantedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleGrantedEvent(e protocolrpc.EventInfo) (*RoleGrantedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleGrantedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForRoleRevokedEvent waits for a RoleRevokedEvent event.
func (c *AdvancedPoolHooksClient) WaitForRoleRevokedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleRevokedEvent) bool) (*RoleRevokedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleRevokedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleRevokedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleRevokedEvent(e protocolrpc.EventInfo) (*RoleRevokedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleRevokedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerAddedEvent waits for a AuthorizedCallerAddedEvent event.
func (c *AdvancedPoolHooksClient) WaitForAuthorizedCallerAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerAddedEvent) bool) (*AuthorizedCallerAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerAddedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerRemovedEvent waits for a AuthorizedCallerRemovedEvent event.
func (c *AdvancedPoolHooksClient) WaitForAuthorizedCallerRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerRemovedEvent) bool) (*AuthorizedCallerRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerRemovedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransferStartedEvent waits for a OwnershipTransferStartedEvent event.
func (c *AdvancedPoolHooksClient) WaitForOwnershipTransferStartedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferStartedEvent) bool) (*OwnershipTransferStartedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferStartedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipTransferStartedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipTransferStartedEvent(e protocolrpc.EventInfo) (*OwnershipTransferStartedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferStartedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "previous_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.PreviousOwner = v
			}
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}

// WaitForAllowListAddEvent waits for a AllowListAddEvent event.
func (c *AdvancedPoolHooksClient) WaitForAllowListAddEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AllowListAddEvent) bool) (*AllowListAddEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AllowListAddEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAllowListAddEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAllowListAddEvent(e protocolrpc.EventInfo) (*AllowListAddEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AllowListAddEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForAllowListRemoveEvent waits for a AllowListRemoveEvent event.
func (c *AdvancedPoolHooksClient) WaitForAllowListRemoveEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AllowListRemoveEvent) bool) (*AllowListRemoveEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AllowListRemoveEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAllowListRemoveEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAllowListRemoveEvent(e protocolrpc.EventInfo) (*AllowListRemoveEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AllowListRemoveEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForCCVConfigUpdatedEvent waits for a CCVConfigUpdatedEvent event.
func (c *AdvancedPoolHooksClient) WaitForCCVConfigUpdatedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CCVConfigUpdatedEvent) bool) (*CCVConfigUpdatedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CCVConfigUpdatedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseCCVConfigUpdatedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseCCVConfigUpdatedEvent(e protocolrpc.EventInfo) (*CCVConfigUpdatedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CCVConfigUpdatedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "remote_chain_selector":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.RemoteChainSelector = v
			}
		case "config":
			v, err := CCVConfigFromScVal(entry.Val)
			if err == nil {
				result.Config = *v
			}
		}
	}

	return result, nil
}

// WaitForThresholdAmountSetEvent waits for a ThresholdAmountSetEvent event.
func (c *AdvancedPoolHooksClient) WaitForThresholdAmountSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*ThresholdAmountSetEvent) bool) (*ThresholdAmountSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{ThresholdAmountSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseThresholdAmountSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseThresholdAmountSetEvent(e protocolrpc.EventInfo) (*ThresholdAmountSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &ThresholdAmountSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "threshold_amount":
			v, err := scval.I128FromScVal(entry.Val)
			if err == nil {
				result.ThresholdAmount = v
			}
		}
	}

	return result, nil
}
