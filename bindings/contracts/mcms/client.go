// Code generated by stellar-bindings-generator. DO NOT EDIT.
package mcms

import (
	"context"
	"fmt"
	"time"

	"github.com/smartcontractkit/chainlink-stellar/bindings"
	"github.com/smartcontractkit/chainlink-stellar/bindings/scval"
	protocolrpc "github.com/stellar/go-stellar-sdk/protocols/rpc"
	"github.com/stellar/go-stellar-sdk/xdr"
)

// McmsClient provides methods to interact with the Mcms contract.
type McmsClient struct {
	invoker    bindings.Invoker
	contractID string
}

// NewMcmsClient creates a new McmsClient.
func NewMcmsClient(invoker bindings.Invoker, contractID string) *McmsClient {
	return &McmsClient{
		invoker:    invoker,
		contractID: contractID,
	}
}

// ContractID returns the contract ID.
func (c *McmsClient) ContractID() string {
	return c.contractID
}

// Owner calls the owner function on the contract.
func (c *McmsClient) Owner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// Execute calls the execute function on the contract.
func (c *McmsClient) Execute(ctx context.Context, op StellarOp, proof MerkleProof) error {
	args := []xdr.ScVal{
		scval.MustToScVal(op.ToScVal()),
		scval.MustToScVal(proof.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "execute", args)
	if err != nil {
		return fmt.Errorf("failed to call execute: %w", err)
	}

	_ = result // void return
	return nil
}

// GetRoot calls the get_root function on the contract.
func (c *McmsClient) GetRoot(ctx context.Context) ([32]byte, uint32, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_root", args)
	if err != nil {
		return [32]byte{}, 0, fmt.Errorf("failed to call get_root: %w", err)
	}

	if result == nil {
		return [32]byte{}, 0, fmt.Errorf("no return value from get_root")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return [32]byte{}, 0, fmt.Errorf("expected vec for tuple return")
	}
	if len(*vec) != 2 {
		return [32]byte{}, 0, fmt.Errorf("expected 2 elements, got %d", len(*vec))
	}

	v0, err := scval.Bytes32FromScVal((*vec)[0])
	if err != nil {
		return [32]byte{}, 0, fmt.Errorf("tuple[0]: %w", err)
	}

	v1Raw, ok := (*vec)[1].GetU32()
	if !ok {
		return [32]byte{}, 0, fmt.Errorf("tuple[1]: expected u32")
	}
	v1 := uint32(v1Raw)

	return v0, v1, nil
}

// IsOwner calls the is_owner function on the contract.
func (c *McmsClient) IsOwner(ctx context.Context, addr string) (bool, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(addr),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "is_owner", args)
	if err != nil {
		return false, fmt.Errorf("failed to call is_owner: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from is_owner")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// SetRoot calls the set_root function on the contract.
func (c *McmsClient) SetRoot(ctx context.Context, root [32]byte, validUntil uint32, metadata StellarRootMetadata, metadataProof MerkleProof, signatures SignatureVec) error {
	args := []xdr.ScVal{
		scval.Bytes32ToScVal(root),
		scval.Uint32ToScVal(validUntil),
		scval.MustToScVal(metadata.ToScVal()),
		scval.MustToScVal(metadataProof.ToScVal()),
		scval.MustToScVal(signatures.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_root", args)
	if err != nil {
		return fmt.Errorf("failed to call set_root: %w", err)
	}

	_ = result // void return
	return nil
}

// GetConfig calls the get_config function on the contract.
func (c *McmsClient) GetConfig(ctx context.Context) (*Config, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_config")
	}

	return ConfigFromScVal(*result)
}

// InitOwner calls the init_owner function on the contract.
func (c *McmsClient) InitOwner(ctx context.Context, owner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "init_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call init_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// Initialize calls the initialize function on the contract.
func (c *McmsClient) Initialize(ctx context.Context, owner string, chainNetworkId [32]byte) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
		scval.Bytes32ToScVal(chainNetworkId),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "initialize", args)
	if err != nil {
		return fmt.Errorf("failed to call initialize: %w", err)
	}

	_ = result // void return
	return nil
}

// SetConfig calls the set_config function on the contract.
func (c *McmsClient) SetConfig(ctx context.Context, signerAddresses SignerAddresses, signerGroups SignerGroups, groupQuorums [32]byte, groupParents [32]byte, clearRoot bool) error {
	args := []xdr.ScVal{
		scval.MustToScVal(signerAddresses.ToScVal()),
		scval.MustToScVal(signerGroups.ToScVal()),
		scval.Bytes32ToScVal(groupQuorums),
		scval.Bytes32ToScVal(groupParents),
		scval.BoolToScVal(clearRoot),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_config", args)
	if err != nil {
		return fmt.Errorf("failed to call set_config: %w", err)
	}

	_ = result // void return
	return nil
}

// GetOpCount calls the get_op_count function on the contract.
func (c *McmsClient) GetOpCount(ctx context.Context) (uint64, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_op_count", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_op_count: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_op_count")
	}

	v, err := scval.Uint64FromScVal(*result)
	if err != nil {
		return 0, err
	}
	return v, nil
}

// RequireOwner calls the require_owner function on the contract.
func (c *McmsClient) RequireOwner(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "require_owner", args)
	if err != nil {
		return "", fmt.Errorf("failed to call require_owner: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from require_owner")
	}

	v, err := scval.AddressFromScVal(*result)
	if err != nil {
		return "", err
	}
	return v, nil
}

// SetNewOwner calls the set_new_owner function on the contract.
func (c *McmsClient) SetNewOwner(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_new_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call set_new_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// ExtendAllTtls calls the extend_all_ttls function on the contract.
func (c *McmsClient) ExtendAllTtls(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "extend_all_ttls", args)
	if err != nil {
		return fmt.Errorf("failed to call extend_all_ttls: %w", err)
	}

	_ = result // void return
	return nil
}

// AcceptOwnership calls the accept_ownership function on the contract.
func (c *McmsClient) AcceptOwnership(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "accept_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call accept_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// ChainNetworkId calls the chain_network_id function on the contract.
func (c *McmsClient) ChainNetworkId(ctx context.Context) ([32]byte, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "chain_network_id", args)
	if err != nil {
		return [32]byte{}, fmt.Errorf("failed to call chain_network_id: %w", err)
	}

	if result == nil {
		return [32]byte{}, fmt.Errorf("no return value from chain_network_id")
	}

	v, err := scval.Bytes32FromScVal(*result)
	if err != nil {
		return [32]byte{}, err
	}
	return v, nil
}

// GetPendingOwner calls the get_pending_owner function on the contract.
func (c *McmsClient) GetPendingOwner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_pending_owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_pending_owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_pending_owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetRootMetadata calls the get_root_metadata function on the contract.
func (c *McmsClient) GetRootMetadata(ctx context.Context) (*StellarRootMetadata, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_root_metadata", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_root_metadata: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_root_metadata")
	}

	return StellarRootMetadataFromScVal(*result)
}

// TransferOwnership calls the transfer_ownership function on the contract.
func (c *McmsClient) TransferOwnership(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "transfer_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call transfer_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// GetMinSecsPerLedger calls the get_min_secs_per_ledger function on the contract.
func (c *McmsClient) GetMinSecsPerLedger(ctx context.Context) (uint64, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_min_secs_per_ledger", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_min_secs_per_ledger: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_min_secs_per_ledger")
	}

	v, err := scval.Uint64FromScVal(*result)
	if err != nil {
		return 0, err
	}
	return v, nil
}

// SetMinSecsPerLedger calls the set_min_secs_per_ledger function on the contract.
func (c *McmsClient) SetMinSecsPerLedger(ctx context.Context, secs uint64) error {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(secs),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_min_secs_per_ledger", args)
	if err != nil {
		return fmt.Errorf("failed to call set_min_secs_per_ledger: %w", err)
	}

	_ = result // void return
	return nil
}

// CancelOwnershipTransfer calls the cancel_ownership_transfer function on the contract.
func (c *McmsClient) CancelOwnershipTransfer(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "cancel_ownership_transfer", args)
	if err != nil {
		return fmt.Errorf("failed to call cancel_ownership_transfer: %w", err)
	}

	_ = result // void return
	return nil
}

// WaitForRoleGrantedEvent waits for a RoleGrantedEvent event.
func (c *McmsClient) WaitForRoleGrantedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleGrantedEvent) bool) (*RoleGrantedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleGrantedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleGrantedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleGrantedEvent(e protocolrpc.EventInfo) (*RoleGrantedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleGrantedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForRoleRevokedEvent waits for a RoleRevokedEvent event.
func (c *McmsClient) WaitForRoleRevokedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleRevokedEvent) bool) (*RoleRevokedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleRevokedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseRoleRevokedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseRoleRevokedEvent(e protocolrpc.EventInfo) (*RoleRevokedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleRevokedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerAddedEvent waits for a AuthorizedCallerAddedEvent event.
func (c *McmsClient) WaitForAuthorizedCallerAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerAddedEvent) bool) (*AuthorizedCallerAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerAddedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerRemovedEvent waits for a AuthorizedCallerRemovedEvent event.
func (c *McmsClient) WaitForAuthorizedCallerRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerRemovedEvent) bool) (*AuthorizedCallerRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseAuthorizedCallerRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseAuthorizedCallerRemovedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransferStartedEvent waits for a OwnershipTransferStartedEvent event.
func (c *McmsClient) WaitForOwnershipTransferStartedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferStartedEvent) bool) (*OwnershipTransferStartedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferStartedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOwnershipTransferStartedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOwnershipTransferStartedEvent(e protocolrpc.EventInfo) (*OwnershipTransferStartedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferStartedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "previous_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.PreviousOwner = v
			}
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}

// WaitForNewRootEvent waits for a NewRootEvent event.
func (c *McmsClient) WaitForNewRootEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*NewRootEvent) bool) (*NewRootEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{NewRootEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseNewRootEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseNewRootEvent(e protocolrpc.EventInfo) (*NewRootEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &NewRootEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "root":
			v, err := scval.Bytes32FromScVal(entry.Val)
			if err == nil {
				result.Root = v
			}
		case "valid_until":
			v, ok := entry.Val.GetU32()
			if ok {
				result.ValidUntil = uint32(v)
			}
		case "metadata":
			v, err := StellarRootMetadataFromScVal(entry.Val)
			if err == nil {
				result.Metadata = *v
			}
		}
	}

	return result, nil
}

// WaitForConfigSetEvent waits for a ConfigSetEvent event.
func (c *McmsClient) WaitForConfigSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*ConfigSetEvent) bool) (*ConfigSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{ConfigSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseConfigSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseConfigSetEvent(e protocolrpc.EventInfo) (*ConfigSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &ConfigSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "config":
			v, err := ConfigFromScVal(entry.Val)
			if err == nil {
				result.Config = *v
			}
		case "is_root_cleared":
			v, ok := entry.Val.GetB()
			if ok {
				result.IsRootCleared = v
			}
		}
	}

	return result, nil
}

// WaitForOpExecutedEvent waits for a OpExecutedEvent event.
func (c *McmsClient) WaitForOpExecutedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OpExecutedEvent) bool) (*OpExecutedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OpExecutedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseOpExecutedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseOpExecutedEvent(e protocolrpc.EventInfo) (*OpExecutedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OpExecutedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "nonce":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.Nonce = v
			}
		case "to":
			v, err := scval.Bytes32FromScVal(entry.Val)
			if err == nil {
				result.To = v
			}
		case "data":
			v, ok := entry.Val.GetBytes()
			if ok {
				result.Data = []byte(v)
			}
		case "value":
			v, err := scval.Bytes32FromScVal(entry.Val)
			if err == nil {
				result.Value = v
			}
		}
	}

	return result, nil
}

// WaitForMinSecsPerLedgerSetEvent waits for a MinSecsPerLedgerSetEvent event.
func (c *McmsClient) WaitForMinSecsPerLedgerSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*MinSecsPerLedgerSetEvent) bool) (*MinSecsPerLedgerSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{MinSecsPerLedgerSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := ParseMinSecsPerLedgerSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func ParseMinSecsPerLedgerSetEvent(e protocolrpc.EventInfo) (*MinSecsPerLedgerSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &MinSecsPerLedgerSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "min_secs_per_ledger":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.MinSecsPerLedger = v
			}
		}
	}

	return result, nil
}
