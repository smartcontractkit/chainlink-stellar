// Code generated by stellar-bindings-generator. DO NOT EDIT.
package onramp

import (
	"context"
	"fmt"
	"time"

	"github.com/smartcontractkit/chainlink-stellar/bindings"
	"github.com/smartcontractkit/chainlink-stellar/bindings/scval"
	protocolrpc "github.com/stellar/go-stellar-sdk/protocols/rpc"
	"github.com/stellar/go-stellar-sdk/xdr"
)

// OnRampClient provides methods to interact with the OnRamp contract.
type OnRampClient struct {
	invoker    bindings.Invoker
	contractID string
}

// NewOnRampClient creates a new OnRampClient.
func NewOnRampClient(invoker bindings.Invoker, contractID string) *OnRampClient {
	return &OnRampClient{
		invoker:    invoker,
		contractID: contractID,
	}
}

// ContractID returns the contract ID.
func (c *OnRampClient) ContractID() string {
	return c.contractID
}

// Init calls the init function on the contract.
func (c *OnRampClient) Init(ctx context.Context, rmnProxy string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(rmnProxy),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "init", args)
	if err != nil {
		return fmt.Errorf("failed to call init: %w", err)
	}

	_ = result // void return
	return nil
}

// Owner calls the owner function on the contract.
func (c *OnRampClient) Owner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetFee calls the get_fee function on the contract.
func (c *OnRampClient) GetFee(ctx context.Context, destChainSelector uint64, message StellarToAnyMessage) (int64, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
		scval.MustToScVal(message.ToScVal()),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_fee", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_fee: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_fee")
	}

	v, err := scval.I128FromScVal(*result)
	if err != nil {
		return 0, err
	}
	return v, nil
}

// IsOwner calls the is_owner function on the contract.
func (c *OnRampClient) IsOwner(ctx context.Context, addr string) (bool, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(addr),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "is_owner", args)
	if err != nil {
		return false, fmt.Errorf("failed to call is_owner: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from is_owner")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// IsCursed calls the is_cursed function on the contract.
func (c *OnRampClient) IsCursed(ctx context.Context) (bool, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "is_cursed", args)
	if err != nil {
		return false, fmt.Errorf("failed to call is_cursed: %w", err)
	}

	if result == nil {
		return false, fmt.Errorf("no return value from is_cursed")
	}

	v, ok := result.GetB()
	if !ok {
		return false, fmt.Errorf("expected bool return type")
	}
	return v, nil
}

// InitOwner calls the init_owner function on the contract.
func (c *OnRampClient) InitOwner(ctx context.Context, owner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "init_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call init_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// Initialize calls the initialize function on the contract.
func (c *OnRampClient) Initialize(ctx context.Context, owner string, staticConfig StaticConfig, dynamicConfig DynamicConfig) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(owner),
		scval.MustToScVal(staticConfig.ToScVal()),
		scval.MustToScVal(dynamicConfig.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "initialize", args)
	if err != nil {
		return fmt.Errorf("failed to call initialize: %w", err)
	}

	_ = result // void return
	return nil
}

// RequireOwner calls the require_owner function on the contract.
func (c *OnRampClient) RequireOwner(ctx context.Context) (string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "require_owner", args)
	if err != nil {
		return "", fmt.Errorf("failed to call require_owner: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from require_owner")
	}

	v, err := scval.AddressFromScVal(*result)
	if err != nil {
		return "", err
	}
	return v, nil
}

// SetNewOwner calls the set_new_owner function on the contract.
func (c *OnRampClient) SetNewOwner(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_new_owner", args)
	if err != nil {
		return fmt.Errorf("failed to call set_new_owner: %w", err)
	}

	_ = result // void return
	return nil
}

// AcceptOwnership calls the accept_ownership function on the contract.
func (c *OnRampClient) AcceptOwnership(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "accept_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call accept_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// GetPendingOwner calls the get_pending_owner function on the contract.
func (c *OnRampClient) GetPendingOwner(ctx context.Context) (*string, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_pending_owner", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_pending_owner: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_pending_owner")
	}

	v, err := scval.OptionalAddressFromScVal(*result)
	if err != nil {
		return nil, err
	}
	return v, nil
}

// GetStaticConfig calls the get_static_config function on the contract.
func (c *OnRampClient) GetStaticConfig(ctx context.Context) (*StaticConfig, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_static_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_static_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_static_config")
	}

	return StaticConfigFromScVal(*result)
}

// GetDynamicConfig calls the get_dynamic_config function on the contract.
func (c *OnRampClient) GetDynamicConfig(ctx context.Context) (*DynamicConfig, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_dynamic_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_dynamic_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_dynamic_config")
	}

	return DynamicConfigFromScVal(*result)
}

// RequireNotCursed calls the require_not_cursed function on the contract.
func (c *OnRampClient) RequireNotCursed(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "require_not_cursed", args)
	if err != nil {
		return fmt.Errorf("failed to call require_not_cursed: %w", err)
	}

	_ = result // void return
	return nil
}

// SetDynamicConfig calls the set_dynamic_config function on the contract.
func (c *OnRampClient) SetDynamicConfig(ctx context.Context, dynamicConfig DynamicConfig) error {
	args := []xdr.ScVal{
		scval.MustToScVal(dynamicConfig.ToScVal()),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "set_dynamic_config", args)
	if err != nil {
		return fmt.Errorf("failed to call set_dynamic_config: %w", err)
	}

	_ = result // void return
	return nil
}

// TransferOwnership calls the transfer_ownership function on the contract.
func (c *OnRampClient) TransferOwnership(ctx context.Context, newOwner string) error {
	args := []xdr.ScVal{
		scval.AddressToScVal(newOwner),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "transfer_ownership", args)
	if err != nil {
		return fmt.Errorf("failed to call transfer_ownership: %w", err)
	}

	_ = result // void return
	return nil
}

// ForwardFromRouter calls the forward_from_router function on the contract.
func (c *OnRampClient) ForwardFromRouter(ctx context.Context, destChainSelector uint64, message StellarToAnyMessage, feeTokenAmount int64, originalSender string) ([32]byte, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
		scval.MustToScVal(message.ToScVal()),
		scval.I128ToScVal(feeTokenAmount),
		scval.AddressToScVal(originalSender),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "forward_from_router", args)
	if err != nil {
		return [32]byte{}, fmt.Errorf("failed to call forward_from_router: %w", err)
	}

	if result == nil {
		return [32]byte{}, fmt.Errorf("no return value from forward_from_router")
	}

	v, err := scval.Bytes32FromScVal(*result)
	if err != nil {
		return [32]byte{}, err
	}
	return v, nil
}

// WithdrawFeeTokens calls the withdraw_fee_tokens function on the contract.
func (c *OnRampClient) WithdrawFeeTokens(ctx context.Context, feeTokens []string) error {
	args := []xdr.ScVal{
		scval.AddressSliceToScVal(feeTokens),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "withdraw_fee_tokens", args)
	if err != nil {
		return fmt.Errorf("failed to call withdraw_fee_tokens: %w", err)
	}

	_ = result // void return
	return nil
}

// GetDestChainConfig calls the get_dest_chain_config function on the contract.
func (c *OnRampClient) GetDestChainConfig(ctx context.Context, destChainSelector uint64) (*DestChainConfig, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_dest_chain_config", args)
	if err != nil {
		return nil, fmt.Errorf("failed to call get_dest_chain_config: %w", err)
	}

	if result == nil {
		return nil, fmt.Errorf("no return value from get_dest_chain_config")
	}

	return DestChainConfigFromScVal(*result)
}

// GetPoolBySourceToken calls the get_pool_by_source_token function on the contract.
func (c *OnRampClient) GetPoolBySourceToken(ctx context.Context, sourceToken string) (string, error) {
	args := []xdr.ScVal{
		scval.AddressToScVal(sourceToken),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_pool_by_source_token", args)
	if err != nil {
		return "", fmt.Errorf("failed to call get_pool_by_source_token: %w", err)
	}

	if result == nil {
		return "", fmt.Errorf("no return value from get_pool_by_source_token")
	}

	v, err := scval.AddressFromScVal(*result)
	if err != nil {
		return "", err
	}
	return v, nil
}

// CancelOwnershipTransfer calls the cancel_ownership_transfer function on the contract.
func (c *OnRampClient) CancelOwnershipTransfer(ctx context.Context) error {
	args := []xdr.ScVal{}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "cancel_ownership_transfer", args)
	if err != nil {
		return fmt.Errorf("failed to call cancel_ownership_transfer: %w", err)
	}

	_ = result // void return
	return nil
}

// GetAllDestChainConfigs calls the get_all_dest_chain_configs function on the contract.
func (c *OnRampClient) GetAllDestChainConfigs(ctx context.Context) ([]uint64, []DestChainConfig, error) {
	args := []xdr.ScVal{}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_all_dest_chain_configs", args)
	if err != nil {
		return nil, nil, fmt.Errorf("failed to call get_all_dest_chain_configs: %w", err)
	}

	if result == nil {
		return nil, nil, fmt.Errorf("no return value from get_all_dest_chain_configs")
	}

	vec, ok := result.GetVec()
	if !ok || vec == nil {
		return nil, nil, fmt.Errorf("expected vec for tuple return")
	}
	if len(*vec) != 2 {
		return nil, nil, fmt.Errorf("expected 2 elements, got %d", len(*vec))
	}

	v0Vec, ok := (*vec)[0].GetVec()
	if !ok || v0Vec == nil {
		return nil, nil, fmt.Errorf("tuple[0]: expected vec")
	}
	v0 := make([]uint64, len(*v0Vec))
	for i, item := range *v0Vec {
		val, err := scval.Uint64FromScVal(item)
		if err != nil {
			return nil, nil, fmt.Errorf("tuple[0][%d]: %w", i, err)
		}
		v0[i] = val
	}

	v1Vec, ok := (*vec)[1].GetVec()
	if !ok || v1Vec == nil {
		return nil, nil, fmt.Errorf("tuple[1]: expected vec")
	}
	v1 := make([]DestChainConfig, len(*v1Vec))
	for i, item := range *v1Vec {
		val, err := DestChainConfigFromScVal(item)
		if err != nil {
			return nil, nil, fmt.Errorf("tuple[1][%d]: %w", i, err)
		}
		v1[i] = *val
	}

	return v0, v1, nil
}

// ApplyDestChainConfigUpdates calls the apply_dest_chain_config_updates function on the contract.
func (c *OnRampClient) ApplyDestChainConfigUpdates(ctx context.Context, destChainConfigArgs []DestChainConfigArgs) error {
	args := []xdr.ScVal{
		scval.StructSliceToScVal(destChainConfigArgs),
	}

	result, err := c.invoker.InvokeContract(ctx, c.contractID, "apply_dest_chain_config_updates", args)
	if err != nil {
		return fmt.Errorf("failed to call apply_dest_chain_config_updates: %w", err)
	}

	_ = result // void return
	return nil
}

// GetExpectedNextMessageNumber calls the get_expected_next_message_number function on the contract.
func (c *OnRampClient) GetExpectedNextMessageNumber(ctx context.Context, destChainSelector uint64) (uint64, error) {
	args := []xdr.ScVal{
		scval.Uint64ToScVal(destChainSelector),
	}

	result, err := c.invoker.SimulateContract(ctx, c.contractID, "get_expected_next_message_number", args)
	if err != nil {
		return 0, fmt.Errorf("failed to call get_expected_next_message_number: %w", err)
	}

	if result == nil {
		return 0, fmt.Errorf("no return value from get_expected_next_message_number")
	}

	v, err := scval.Uint64FromScVal(*result)
	if err != nil {
		return 0, err
	}
	return v, nil
}

// WaitForRoleGrantedEvent waits for a RoleGrantedEvent event.
func (c *OnRampClient) WaitForRoleGrantedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleGrantedEvent) bool) (*RoleGrantedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleGrantedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseRoleGrantedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseRoleGrantedEvent(e protocolrpc.EventInfo) (*RoleGrantedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleGrantedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForRoleRevokedEvent waits for a RoleRevokedEvent event.
func (c *OnRampClient) WaitForRoleRevokedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*RoleRevokedEvent) bool) (*RoleRevokedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{RoleRevokedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseRoleRevokedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseRoleRevokedEvent(e protocolrpc.EventInfo) (*RoleRevokedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &RoleRevokedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "role":
			v, err := scval.SymbolFromScVal(entry.Val)
			if err == nil {
				result.Role = v
			}
		case "account":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Account = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerAddedEvent waits for a AuthorizedCallerAddedEvent event.
func (c *OnRampClient) WaitForAuthorizedCallerAddedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerAddedEvent) bool) (*AuthorizedCallerAddedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerAddedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseAuthorizedCallerAddedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseAuthorizedCallerAddedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerAddedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerAddedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForAuthorizedCallerRemovedEvent waits for a AuthorizedCallerRemovedEvent event.
func (c *OnRampClient) WaitForAuthorizedCallerRemovedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*AuthorizedCallerRemovedEvent) bool) (*AuthorizedCallerRemovedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{AuthorizedCallerRemovedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseAuthorizedCallerRemovedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseAuthorizedCallerRemovedEvent(e protocolrpc.EventInfo) (*AuthorizedCallerRemovedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &AuthorizedCallerRemovedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "caller":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Caller = v
			}
		}
	}

	return result, nil
}

// WaitForOwnershipTransferStartedEvent waits for a OwnershipTransferStartedEvent event.
func (c *OnRampClient) WaitForOwnershipTransferStartedEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferStartedEvent) bool) (*OwnershipTransferStartedEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferStartedEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseOwnershipTransferStartedEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseOwnershipTransferStartedEvent(e protocolrpc.EventInfo) (*OwnershipTransferStartedEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferStartedEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "previous_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.PreviousOwner = v
			}
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}

// WaitForConfigSetEvent waits for a ConfigSetEvent event.
func (c *OnRampClient) WaitForConfigSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*ConfigSetEvent) bool) (*ConfigSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{ConfigSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseConfigSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseConfigSetEvent(e protocolrpc.EventInfo) (*ConfigSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &ConfigSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "static_config":
			// TODO: parse complex type
		case "dynamic_config":
			// TODO: parse complex type
		}
	}

	return result, nil
}

// WaitForCCIPMessageSentEvent waits for a CCIPMessageSentEvent event.
func (c *OnRampClient) WaitForCCIPMessageSentEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*CCIPMessageSentEvent) bool) (*CCIPMessageSentEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{CCIPMessageSentEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseCCIPMessageSentEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseCCIPMessageSentEvent(e protocolrpc.EventInfo) (*CCIPMessageSentEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &CCIPMessageSentEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "dest_chain_selector":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.DestChainSelector = v
			}
		case "sequence_number":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.SequenceNumber = v
			}
		case "sender":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.Sender = v
			}
		case "message_id":
			v, err := scval.Bytes32FromScVal(entry.Val)
			if err == nil {
				result.MessageId = v
			}
		case "fee_token":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.FeeToken = v
			}
		case "token_amount_before_fees":
			v, err := scval.I128FromScVal(entry.Val)
			if err == nil {
				result.TokenAmountBeforeFees = v
			}
		case "encoded_message":
			v, ok := entry.Val.GetBytes()
			if ok {
				result.EncodedMessage = []byte(v)
			}
		case "receipts":
			vec, ok := entry.Val.GetVec()
			if ok && vec != nil {
				parsed := make([]Receipt, 0, len(*vec))
				for _, item := range *vec {
					v, err := ReceiptFromScVal(item)
					if err == nil {
						parsed = append(parsed, *v)
					}
				}
				result.Receipts = parsed
			}
		case "verifier_blobs":
			vec, ok := entry.Val.GetVec()
			if ok && vec != nil {
				parsed := make([][]byte, len(*vec))
				for i, item := range *vec {
					v, ok := item.GetBytes()
					if ok {
						parsed[i] = []byte(v)
					}
				}
				result.VerifierBlobs = parsed
			}
		}
	}

	return result, nil
}

// WaitForDestChainConfigSetEvent waits for a DestChainConfigSetEvent event.
func (c *OnRampClient) WaitForDestChainConfigSetEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*DestChainConfigSetEvent) bool) (*DestChainConfigSetEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{DestChainConfigSetEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseDestChainConfigSetEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseDestChainConfigSetEvent(e protocolrpc.EventInfo) (*DestChainConfigSetEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &DestChainConfigSetEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "dest_chain_selector":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.DestChainSelector = v
			}
		case "message_number":
			v, err := scval.Uint64FromScVal(entry.Val)
			if err == nil {
				result.MessageNumber = v
			}
		case "config":
			// TODO: parse complex type
		}
	}

	return result, nil
}

// WaitForOwnershipTransferredEvent waits for a OwnershipTransferredEvent event.
func (c *OnRampClient) WaitForOwnershipTransferredEvent(ctx context.Context, startLedger uint32, timeout time.Duration, filter func(*OwnershipTransferredEvent) bool) (*OwnershipTransferredEvent, error) {
	startTime := time.Now()
	ticker := time.NewTicker(2 * time.Second)
	defer ticker.Stop()

	for {
		select {
		case <-ctx.Done():
			return nil, ctx.Err()
		case <-ticker.C:
			if time.Since(startTime) > timeout {
				return nil, fmt.Errorf("timeout waiting for event")
			}

			events, err := c.invoker.GetEvents(ctx, c.contractID, startLedger, []string{OwnershipTransferredEventTopic})
			if err != nil {
				continue
			}

			for _, e := range events {
				parsed, err := parseOwnershipTransferredEvent(e)
				if err != nil {
					continue
				}
				if filter == nil || filter(parsed) {
					return parsed, nil
				}
			}
		}
	}
}

func parseOwnershipTransferredEvent(e protocolrpc.EventInfo) (*OwnershipTransferredEvent, error) {
	var eventVal xdr.ScVal
	if err := xdr.SafeUnmarshalBase64(e.ValueXDR, &eventVal); err != nil {
		return nil, fmt.Errorf("failed to decode event: %w", err)
	}

	scMap, ok := eventVal.GetMap()
	if !ok || scMap == nil {
		return nil, fmt.Errorf("event is not a map")
	}

	result := &OwnershipTransferredEvent{
		Ledger: uint32(e.Ledger),
		TxHash: e.TransactionHash,
	}

	for _, entry := range *scMap {
		key, ok := entry.Key.GetSym()
		if !ok {
			continue
		}

		switch string(key) {
		case "new_owner":
			v, err := scval.AddressFromScVal(entry.Val)
			if err == nil {
				result.NewOwner = v
			}
		}
	}

	return result, nil
}

